<header class="flex justify-between items-center m-5 sm:mx-10 sm:my-6">

  <div>
    <%= link_to image_tag('logo.png'), root_path %>
  </div>

  <div>
  <% if user_signed_in? %>
    <div class="flex space-x-2 sm:space-x-4">
      <%= link_to image_tag('search.png'), root_path %>
      <%= link_to image_tag('message.png'), rooms_path %>
      <%= link_to image_tag('alert.png') , notifications_path %>
      <%= link_to image_tag('menu.png'), menu_path %>
    </div>
  <% else %>

    <%# TODO: SPは済み。PC版のレスポンシブ対応はこれから %>
    <div class="flex space-x-2 sm:space-x-4">
      <%= link_to "ログイン", new_user_session_path, class: "px-3.5 py-2 border sm:border-2 border-primary rounded-2xl text-sm text-primary font-medium leading-3" %>
      <%= link_to "会員登録", new_user_registration_path, class: "px-3.5 py-2 bg-primary text-white text-sm rounded-2xl font-medium leading-3" %>
    </div>

  <% end %>

  </div>

</header>